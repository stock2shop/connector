{
  "channel_product_code": "{{ id }}",
  "channel_variant_code": "{{ id }}",
  "price": "{{# getPriceWithoutDiscount}}{{price_with_tax}}-{{total_tax}}-{{total_discount}}{{/ getPriceWithoutDiscount}}",
  "qty": "{{ qty }}",
  "sku": "{{ sku }}",
  "title": "{{ name }}",
  "tax_lines": [
    {
      "rate": "{{ tax_rate }}",
      "title": "{{ name }}",
      "price": "{{# getPriceWithoutTax}} {{price}} - ({{ price }} * ({{tax_rate}} / 100)) {{/ getPriceWithoutTax}}"
    }
  ]
}